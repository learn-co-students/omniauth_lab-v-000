class User < ActiveRecord::Base

  # def self.find_or_create_from_omniauth(auth_hash)
  #   self.where(:email => auth_hash["email"]).first_or_create do |user|
  #     user.password_digest = auth_hash["password"].authenticate
  #   end
  # end

end
