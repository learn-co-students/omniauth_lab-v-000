<a href='/auth/facebook'>Login with Facebook</a>

<% if session[:user_id] %>
  <h1><%= @user.name %></h1>
  <h2>Facebook UID: <%= @user.uid %></h2>
<% else %>
    <a href='/auth/facebook'>Login with Facebook</a>
<% end %>
