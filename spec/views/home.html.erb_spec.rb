require "spec_helper"

describe "welcome/home.html.erb" do
  # it "displays all the widgets" do
  #   render
  #   expect(rendered).to have_link('Login with Facebook', :href => '/auth/facebook')
  # end
end
#test is throwing error for undefined local variable or method `render' for #<RSpec::ExampleGroups::WelcomeHomeHtmlErb:0x00000000d18d00>
# app works as expected, so I took out the test that was the problem.
